# Artifact paths recorded in `artifacts/manifest.json`

Artifact paths recorded in `artifacts/manifest.json`

## Usage

``` r
manifest_artifact_paths(what, ctx)
```

## Arguments

- what:

  Replication id.

- ctx:

  Paper context.
