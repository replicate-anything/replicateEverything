# Error condition when a study replication package is not available

Error condition when a study replication package is not available

## Usage

``` r
study_package_error(package, meta, ctx)
```

## Arguments

- package:

  R package name.

- meta:

  Parsed replication metadata.

- ctx:

  Paper context.
