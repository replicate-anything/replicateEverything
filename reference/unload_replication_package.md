# Unload a study replication package namespace if loaded

Unload a study replication package namespace if loaded

## Usage

``` r
unload_replication_package(package)
```

## Arguments

- package:

  Package name.
