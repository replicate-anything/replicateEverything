# Label for progress messages describing the configured Stata dependency probe

Label for progress messages describing the configured Stata dependency
probe

## Usage

``` r
stata_deps_probe_label(study_root, meta = NULL)
```

## Arguments

- study_root:

  Local study repository root.

- meta:

  Optional parsed replication metadata.

## Value

Short character description.
