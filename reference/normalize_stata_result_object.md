# Normalize a Stata result for format functions

Normalize a Stata result for format functions

## Usage

``` r
normalize_stata_result_object(object)
```

## Arguments

- object:

  Stata result list, path, or replication envelope.

## Value

A `stata_replication_result` list when possible.
