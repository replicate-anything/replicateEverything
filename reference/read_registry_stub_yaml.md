# Read the registry stub yaml for a paper folder

Read the registry stub yaml for a paper folder

## Usage

``` r
read_registry_stub_yaml(folder, registry_root = NULL)
```

## Arguments

- folder:

  Registry folder name.

- registry_root:

  Optional registry checkout root.
