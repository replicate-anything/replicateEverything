# Load a package from a local source tree

Load a package from a local source tree

## Usage

``` r
load_replication_package_path(path, package)
```

## Arguments

- path:

  Path to package root (contains DESCRIPTION).

- package:

  Expected package name.
