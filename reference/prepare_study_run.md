# Prepare and validate a study run plan

Prepare and validate a study run plan

## Usage

``` r
prepare_study_run(
  doi,
  what,
  given = "parents",
  format = FALSE,
  force = FALSE,
  repo = NULL,
  folder = NULL
)
```
