# Build the lightweight registry stub yaml list from folder study metadata

Build the lightweight registry stub yaml list from folder study metadata

## Usage

``` r
registry_stub_from_folder_meta(meta, study_folder = NULL, study_root = NULL)
```
