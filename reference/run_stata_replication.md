# Run a Stata-backed replication entry

Run a Stata-backed replication entry

## Usage

``` r
run_stata_replication(rep, ctx, meta = NULL)
```

## Arguments

- rep:

  Replication entry.

- ctx:

  Paper context.

- meta:

  Optional parsed replication metadata for study resolution.

## Value

A `stata_replication_result` list.
