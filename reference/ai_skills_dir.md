# Directory containing bundled AI skill files

Directory containing bundled AI skill files

## Usage

``` r
ai_skills_dir(package = utils::packageName())
```

## Arguments

- package:

  Optional package name. Defaults to the calling package.

## Value

Normalized path, or `""` when no skills are installed.
