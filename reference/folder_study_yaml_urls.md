# URLs for `replication.yml` in a folder-backed study repo

URLs for `replication.yml` in a folder-backed study repo

## Usage

``` r
folder_study_yaml_urls(repo, ref = "main")
```

## Arguments

- repo:

  GitHub slug `org/repo`.

- ref:

  Branch, tag, or commit.
