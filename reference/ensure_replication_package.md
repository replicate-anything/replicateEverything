# Ensure a study replication package is installed

Ensure a study replication package is installed

## Usage

``` r
ensure_replication_package(package, repo)
```

## Arguments

- package:

  R package name.

- repo:

  GitHub slug `org/repo`.
