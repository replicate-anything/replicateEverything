# Build display node records for one path (optional leading raw-data node)

Build display node records for one path (optional leading raw-data node)

## Usage

``` r
dag_path_node_records(path_ids, graph, steps, produced_outputs)
```
