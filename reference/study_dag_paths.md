# Root-to-sink paths within a DAG component (parallel branches, not one linear chain)

Root-to-sink paths within a DAG component (parallel branches, not one
linear chain)

## Usage

``` r
study_dag_paths(comp, graph)
```
