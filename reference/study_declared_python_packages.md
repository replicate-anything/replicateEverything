# Python packages declared in replication.yml

Uses top-level `python_dependencies:` or `paper.python_dependencies:`.
When omitted, aggregates `dependencies:` from python engine entries.

## Usage

``` r
study_declared_python_packages(meta)
```

## Arguments

- meta:

  Parsed replication metadata.
