# Extract the output file path from a Stata replication result

Accepts a `stata_replication_result` list or a plain character path.

## Usage

``` r
stata_result_path(object)
```

## Arguments

- object:

  Stata result list or path to `.smcl`/image output.

## Value

Character path or `NULL`.
