# Resolve the analysis function from a sourced replication script

Resolve the analysis function from a sourced replication script

## Usage

``` r
get_analysis_function(env, what, type)
```

## Arguments

- env:

  Environment containing sourced definitions.

- what:

  Replication identifier.

- type:

  Replication type (`figure` or `table`).
