# Load all sibling replication packages under a monorepo root

Load all sibling replication packages under a monorepo root

## Usage

``` r
load_sibling_replication_packages(root)
```

## Arguments

- root:

  Directory containing `rep_*` package folders.
