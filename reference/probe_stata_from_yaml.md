# Probe Stata from yaml declarations (executable + probe or stata_packages)

Probe Stata from yaml declarations (executable + probe or
stata_packages)

## Usage

``` r
probe_stata_from_yaml(meta, study_root = NULL)
```
