# Whether a replication entry runs in Stata

Whether a replication entry runs in Stata

## Usage

``` r
is_stata_replication(rep, paper_meta = NULL)
```

## Arguments

- rep:

  Replication entry from `replication.yml`.

- paper_meta:

  Optional paper-level metadata.

## Value

Logical.
