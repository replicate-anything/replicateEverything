# Migrate legacy prep/replications yaml to a unified steps block (character yaml)

Migrate legacy prep/replications yaml to a unified steps block
(character yaml)

## Usage

``` r
migrate_legacy_steps_yaml(meta)
```

## Arguments

- meta:

  Parsed replication metadata or path to replication.yml.

## Value

Character scalar containing a `steps:` yaml block.
