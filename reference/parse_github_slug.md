# Parse a GitHub repo slug from a URL or slug string

Parse a GitHub repo slug from a URL or slug string

## Usage

``` r
parse_github_slug(location)
```

## Arguments

- location:

  Character. Local path, HTTPS URL, git URL, or `org/repo`.

## Value

Character `org/repo` or `NULL`.
