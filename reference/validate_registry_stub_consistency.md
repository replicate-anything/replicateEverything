# Compare registry stub fields against full study metadata

Compare registry stub fields against full study metadata

## Usage

``` r
validate_registry_stub_consistency(meta, stub, study_root, kind = NULL)
```
