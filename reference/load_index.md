# Load the replication registry index

Load the replication registry index

## Usage

``` r
load_index()
```

## Value

A data frame containing replication metadata.

## Examples

``` r
if (FALSE) { # \dontrun{
head(load_index())
} # }
```
