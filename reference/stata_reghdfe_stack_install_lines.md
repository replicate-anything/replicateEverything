# Stata install lines for the SSC `ftools` + `reghdfe` stack

Current SSC `reghdfe` (6.x) requires the `require` package. Refreshes
broken partial installs on shared servers.

## Usage

``` r
stata_reghdfe_stack_install_lines()
```
