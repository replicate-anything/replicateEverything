# Repo-relative path variants for one declared output (outputs-only)

Repo-relative path variants for one declared output (outputs-only)

## Usage

``` r
step_output_rel_variants(rel, step_id = "")
```
