# Raw GitHub URLs for a study package R source file

Raw GitHub URLs for a study package R source file

## Usage

``` r
package_replication_code_urls(name, repo, ref = "main")
```

## Arguments

- name:

  Base name without `.R` (e.g. `make_figure_1`).

- repo:

  GitHub slug.

- ref:

  Git ref.
