# Resolve a format function, falling back to a no-op default

Resolve a format function, falling back to a no-op default

## Usage

``` r
resolve_format_function(env, rep, paper_meta = NULL)
```

## Arguments

- env:

  Environment sourced from replication scripts.

- rep:

  Replication entry.

- paper_meta:

  Optional paper-level metadata.
