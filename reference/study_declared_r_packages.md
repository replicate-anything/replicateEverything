# R packages declared in replication.yml

Uses `paper.dependencies` and optional top-level `r_dependencies:`.

## Usage

``` r
study_declared_r_packages(meta)
```

## Arguments

- meta:

  Parsed replication metadata.
