# Infer substantive Stata scripts from nested `do` calls in a runner.

Infer substantive Stata scripts from nested `do` calls in a runner.

## Usage

``` r
infer_stata_source_paths(wrapper_lines)
```
