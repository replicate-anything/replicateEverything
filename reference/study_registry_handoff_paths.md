# Paths to prepared registry handoff files in a study repository

Paths to prepared registry handoff files in a study repository

## Usage

``` r
study_registry_handoff_paths(study_root, kind = NULL)
```
