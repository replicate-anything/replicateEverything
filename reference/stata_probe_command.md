# Stata ado command to probe for a declared package name

`estout` installs `eststo` / `esttab`; probe the runnable command.

## Usage

``` r
stata_probe_command(pkg)
```

## Arguments

- pkg:

  Declared package name from `stata_packages:`.
