# Write `BUNDLE_SHA` for a Shiny deploy directory

Write `BUNDLE_SHA` for a Shiny deploy directory

## Usage

``` r
write_shiny_bundle_sha(dest, package = "replicateEverything")
```

## Arguments

- dest:

  Deploy directory.

- package:

  Package name.

## Value

Invisibly, the SHA written.
