% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_audit_store.R
\name{seed_registry_audit_jobs}
\alias{seed_registry_audit_jobs}
\title{Seed / refresh audit jobs CSV from bake timings, artifacts, and prior RDS}
\usage{
seed_registry_audit_jobs(
  registry_root = NULL,
  index = NULL,
  from_rds = TRUE,
  from_bake = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{registry_root}{Optional registry root.}

\item{index}{Optional registry index (default \code{\link[=load_index]{load_index()}}).}

\item{from_rds}{If \code{TRUE}, migrate rows from \code{audit_latest.rds}
when present.}

\item{from_bake}{If \code{TRUE}, add provisional ok rows from bake timings /
local artifacts for jobs not yet in the CSV.}

\item{verbose}{Print progress.}
}
\value{
Invisibly, list with \code{jobs} path, row counts, and summary paths.
}
\description{
Populates missing study×step rows so the health bar is not empty before the
first full portfolio audit. Existing \code{source = "audit"} rows are kept;
bake/artifact rows only fill gaps (or refresh provisional bake rows).
}
\keyword{internal}
