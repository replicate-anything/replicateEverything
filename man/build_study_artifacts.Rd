% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_study_artifacts.R
\name{build_study_artifacts}
\alias{build_study_artifacts}
\title{Build display artifacts for a folder-backed study}
\usage{
build_study_artifacts(
  location = ".",
  install_deps = TRUE,
  ids = NULL,
  registry_root = NULL,
  force_prep = FALSE
)
}
\arguments{
\item{location}{Local study path or GitHub address. Defaults to \code{"."} when
the working directory contains \code{replication.yml}.}

\item{install_deps}{Logical. Install missing CRAN dependencies when \code{TRUE}.}

\item{ids}{Optional character vector of replication ids to build. When
\code{NULL}, builds every figure and table in \code{replication.yml}.}

\item{registry_root}{Optional registry checkout path for monorepo dev.}

\item{force_prep}{Logical. Re-run prep steps even when outputs already exist.}
}
\value{
Invisibly, a list with \code{artifact_dir}, \code{manifest}, and per-id status.
}
\description{
Runs pipeline prep steps from \code{replication.yml} when present, then every
registered table and figure, saves formatted outputs under \verb{artifacts/}, and
writes \code{artifacts/manifest.json}. Intended to be run from the study
repository root (or pass the path explicitly).
}
\details{
Registry papers use \code{registry/scripts/build_artifacts.R} instead; folder-backed
studies keep materials in their own repository.
}
\examples{
\dontrun{
build_study_artifacts(".", install_deps = TRUE)
}

}
