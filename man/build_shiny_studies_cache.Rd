% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_studies_cache.R
\name{build_shiny_studies_cache}
\alias{build_shiny_studies_cache}
\title{Build and write the Shiny Studies cache artifact}
\usage{
build_shiny_studies_cache(registry_root, index = NULL, metas = NULL)
}
\arguments{
\item{registry_root}{Path to the registry repository.}

\item{index}{Optional index data frame (defaults to compiling from stubs).}

\item{metas}{Optional list of stub metas aligned with \code{index} rows.}
}
\value{
Invisibly, a list with \code{path}, \code{n}, and \code{cache}.
}
\description{
Writes \code{registry/shiny_studies.json} with one record per study: citation
fields, collections, languages/engines, notes flags (data unavailable /
missing engine), related upstream/downstream (dois, titles, urls), article
and study/repo urls, plus a baked \code{ui} block (select / collection
choices) so Shiny startup does not reassemble dropdowns. Intended for the
Shiny Studies tab — no live yaml fetch at list time.
}
\details{
Called from \code{\link[=build_registry_index]{build_registry_index()}}. Gap flags come from stub
\code{notes:} (written by \code{\link[=sync_study_to_registry]{sync_study_to_registry()}}), declared languages
(mathematica → missing engine), and the latest audit snapshot when present.
}
\examples{
\dontrun{
build_shiny_studies_cache("../registry")
}

}
