% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_code.R
\name{get_code_run_advice}
\alias{get_code_run_advice}
\title{How-to-run advice shared by \code{\link[=get_code]{get_code()}} tips and the Code tab setup box}
\usage{
get_code_run_advice(
  engine = NULL,
  type = NULL,
  rep = NULL,
  lines = NULL,
  doi = NULL,
  what = NULL
)
}
\arguments{
\item{engine}{\code{"r"}, \code{"stata"}, or \code{"python"} (default \code{"r"}).}

\item{type}{Optional step type from yaml.}

\item{rep}{Optional replication / step entry (yaml-implied R recipe; Stata/Python path).}

\item{lines}{Optional script lines (so R tips detect generate_* names).}

\item{doi, what}{Optional concrete ids; placeholders when omitted.}
}
\value{
Character vector of advice lines (no \code{get_code() returns...} preamble).
}
\description{
Guidance for using the displayed script (not the package Live Run path).
Does not mention optional \code{sys.nframe()} script footers. Returns a
working-directory note plus a numbered list under \code{"To produce the
<kind>:"}.
}
\keyword{internal}
