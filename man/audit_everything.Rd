% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_everything.R
\name{audit_everything}
\alias{audit_everything}
\title{Audit all registry replications}
\usage{
audit_everything(
  patience = 20,
  index = NULL,
  dois = NULL,
  install_deps = FALSE,
  verbose = TRUE,
  registry_root = NULL
)
}
\arguments{
\item{patience}{Seconds to allow each table or figure before halting that run.
Defaults to \code{20}.}

\item{index}{Registry index data frame; defaults to \code{\link[=load_index]{load_index()}}.}

\item{dois}{Optional character vector of DOIs to audit. When \code{NULL},
audits every row in \code{index}.}

\item{install_deps}{Logical. Passed to \code{\link[=render_replication]{render_replication()}}.}

\item{verbose}{Logical. Print progress messages.}

\item{registry_root}{Optional path to the registry repository. When set,
writes \code{audit_summary.json} (and \code{audit_latest.rds}) there after
the audit completes.}
}
\value{
An object of class \code{audit_everything} with components
\code{results} (data frame), \code{summary}, and metadata.
}
\description{
Walks the replication registry and attempts every table and figure in each
available engine (R and Stata where defined). Failures do not stop the audit;
results are returned in a concise data frame. For a full HTML report, render
\code{audit_everything.qmd} in the
\href{https://github.com/replicate-anything/registry}{registry repository} (see
\code{\link[=audit_everything_qmd]{audit_everything_qmd()}}).
}
\examples{
\dontrun{
audit <- audit_everything(patience = 20, dois = "10.1177/00491241211036161")
print(audit)
}
}
