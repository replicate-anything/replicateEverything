% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_package_artifacts.R,
%   R/build_study_artifacts.R, R/build_study_outputs.R
\name{build_package_artifacts}
\alias{build_package_artifacts}
\alias{build_study_artifacts}
\alias{build_study_outputs}
\title{Build display outputs for a package-backed study}
\usage{
build_package_artifacts(
  package,
  install_deps = TRUE,
  ids = NULL,
  output_dir = NULL,
  force_prep = FALSE,
  only_missing = FALSE
)

build_study_artifacts(
  location = ".",
  install_deps = TRUE,
  ids = NULL,
  registry_root = NULL,
  force_prep = FALSE,
  only_missing = FALSE
)

build_study_outputs(
  location = ".",
  install_deps = TRUE,
  ids = NULL,
  registry_root = NULL,
  output_dir = NULL,
  force_prep = FALSE,
  only_missing = FALSE
)
}
\arguments{
\item{package}{Character name of the installed study package.}

\item{install_deps}{Logical. Install missing CRAN, pip, and Stata dependencies
when \code{TRUE}.}

\item{ids}{Optional character vector of replication ids to build. When
\code{NULL}, builds every figure and table in \code{replication.yml}.}

\item{output_dir}{Optional output directory (package studies only). Defaults to
the package report outputs directory.}

\item{force_prep}{Logical. Re-run prep steps even when outputs already exist.}

\item{only_missing}{Logical. When \code{TRUE}, skip replications whose artifacts
already exist (see \code{\link[=artifact_available]{artifact_available()}}).}

\item{location}{Local study path, GitHub address, or installed package
name. Defaults to \code{"."} when the working directory contains
\code{replication.yml} or \code{DESCRIPTION}.}

\item{registry_root}{Optional registry checkout path for monorepo dev
(folder studies only).}
}
\value{
Invisibly, a list with \code{artifact_dir}, \code{manifest}, and \code{manifest_path}.

Invisibly, a list with \code{artifact_dir}, \code{manifest}, and per-id status.

Invisibly, a list with \code{output_dir}, \code{manifest}, and per-id status.
}
\description{
Runs pipeline prep steps from \code{replication.yml} when present, then every
registered table and figure, and writes formatted outputs plus
\code{manifest.json}. Works for folder-backed studies (\verb{outputs/}) and
package-backed studies (\verb{inst/report/outputs/} or \verb{inst/report/artifacts/}).
}
\section{Functions}{
\itemize{
\item \code{build_package_artifacts()}: Package-backed implementation.

Runs pipeline prep steps from the package \code{replication.yml}, then builds
every registered table and figure into \verb{inst/report/artifacts/} (or
\code{output_dir} when set). Study packages can delegate from \code{build_report()}.

\item \code{build_study_artifacts()}: Folder-backed implementation.

Runs pipeline prep steps from \code{replication.yml} when present, then every
registered table and figure, saves formatted outputs under \verb{outputs/}, and
writes \code{outputs/manifest.json}. Intended to be run from the study
repository root (or pass the path explicitly).

Registry papers use \code{registry/scripts/build_artifacts.R} instead; folder-backed
studies keep materials in their own repository.

}}
\examples{
\dontrun{
build_study_outputs(".", install_deps = TRUE)
build_study_outputs("rep1371journalpone0278337", install_deps = TRUE)
build_study_outputs(".", only_missing = TRUE)
}

}
\seealso{
\code{\link[=build_outputs]{build_outputs()}} for registry-wide or DOI-scoped builds.
}
\keyword{internal}
