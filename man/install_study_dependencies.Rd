% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintainer_dependencies.R
\name{install_study_dependencies}
\alias{install_study_dependencies}
\title{Install dependencies for one folder-backed or registry study}
\usage{
install_study_dependencies(
  location,
  registry_root = NULL,
  repo = NULL,
  folder = NULL,
  from_registry_index = FALSE
)
}
\arguments{
\item{location}{Study DOI, registry handle, local path, or GitHub slug.}

\item{registry_root}{Optional registry checkout for monorepo dev.}

\item{repo, folder}{Optional registry row hints.}

\item{from_registry_index}{Logical. When \code{TRUE} (set by the registry
scope of \code{\link[=install_dependencies]{install_dependencies()}}), never treat blank input or a sibling
folder name in \code{getwd()} as the local study.}
}
\value{
Invisibly \code{TRUE} on success.
}
\description{
Maintainer setup only. Installs declared R CRAN packages, Python pip
packages, and runs study Stata install scripts (\code{install_stata_deps.do})
once. Works for \strong{folder-backed} and \strong{package-backed} registry studies.
Does \strong{not} build display outputs — use \code{\link[=build_study_outputs]{build_study_outputs()}} for that.
}
\details{
Live Run and Shiny probe dependencies only; call this function (or
\code{\link[=install_dependencies]{install_dependencies()}} with \code{what = "everywhere"}) when onboarding a
machine.
}
\seealso{
\code{\link[=install_dependencies]{install_dependencies()}}, \code{\link[=check_study_compatibility]{check_study_compatibility()}},
\code{\link[=build_study_outputs]{build_study_outputs()}}
}
\keyword{internal}
