% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_folder_paper.R
\name{check_and_bake_study}
\alias{check_and_bake_study}
\title{Build outputs and validate a study (contributor)}
\usage{
check_and_bake_study(
  location = ".",
  build_artifacts = TRUE,
  install_deps = TRUE,
  full_replication = FALSE,
  registry_root = NULL
)
}
\arguments{
\item{location}{Study repo path or GitHub address. Defaults to \code{"."}.}

\item{build_artifacts}{If \code{TRUE}, build precomputed outputs first.}

\item{install_deps}{Passed to the build function.}

\item{full_replication}{If \code{TRUE}, also run every table and figure live.}

\item{registry_root}{Optional registry checkout (passed to build/check helpers).}
}
\value{
Invisibly, a checklist result (\code{folder_replication_check} or
\code{package_replication_check}).
}
\description{
Optionally bakes display artifacts with \code{\link[=build_study_outputs]{build_study_outputs()}}, then runs
\code{\link[=check_replication]{check_replication()}}. On success the study is ready for a maintainer to
register it with \code{\link[=sync_study_to_registry]{sync_study_to_registry()}} (stub written only into the
central registry repository).
}
\examples{
\dontrun{
check_and_bake_study(".")
}

}
