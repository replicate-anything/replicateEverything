% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stata_replication.R
\name{stata_deps_probe_plan_from_packages}
\alias{stata_deps_probe_plan_from_packages}
\title{Build a Stata probe from \code{stata_packages:}, with exit-code attribution}
\usage{
stata_deps_probe_plan_from_packages(packages)
}
\arguments{
\item{packages}{Character vector of ado command names.}
}
\value{
List with \code{lines} (character vector of Stata commands) and
\code{code_map} (named character vector: exit code as name, package
name as value).
}
\description{
Uses \code{which} (or \code{findfile} for file-only packages; see
\code{\link[=stata_package_probe_spec]{stata_package_probe_spec()}}) plus \code{help} (and reghdfe runtime checks
when needed). Each check exits with a distinct code so a failing probe run
can be attributed back to the exact package that failed, instead of
blaming every declared package (see \code{\link[=stata_dependencies_satisfied]{stata_dependencies_satisfied()}}).
}
\keyword{internal}
