% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declared_data.R
\name{materialize_declared_data}
\alias{materialize_declared_data}
\title{Materialize remotely declared study data into the study root}
\usage{
materialize_declared_data(
  meta,
  study_root = NULL,
  ctx = NULL,
  force = FALSE,
  paths = NULL
)
}
\arguments{
\item{meta}{Parsed replication metadata, or a study root path / DOI that
\code{\link[=get_replication_meta]{get_replication_meta()}} can resolve.}

\item{study_root}{Local study repository root. When \code{NULL}, uses
\code{ctx$local_root} / \code{\link[=ensure_study_folder_local]{ensure_study_folder_local()}}.}

\item{ctx}{Optional paper context.}

\item{force}{Re-download existing files.}

\item{paths}{Optional character vector of relative paths to materialize;
when set, only matching declared entries are fetched.}
}
\value{
Invisibly, character vector of absolute paths written or already present.
}
\description{
Fetches files listed under \code{dataverse.files} / \code{data_files:} into
their declared local \code{path}s when missing. Not a pipeline step — call
automatically before runs, or explicitly for smoke checks.
}
