% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_folder_paper.R
\name{sync_study_to_registry}
\alias{sync_study_to_registry}
\title{Sync a prepared study into the registry repository (maintainer)}
\usage{
sync_study_to_registry(
  location = ".",
  registry_root = NULL,
  audit = FALSE,
  patience = 20,
  install_deps = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{location}{Study repo path or GitHub address. Defaults to \code{"."}.}

\item{registry_root}{Path to the registry repository root. Defaults to
\code{getOption("replicateEverything.registry_root")}.}

\item{audit}{If \code{TRUE}, run \code{\link[=audit_everything]{audit_everything()}} for this study after sync.}

\item{patience}{Seconds per replication when \code{audit = TRUE}.}

\item{install_deps}{Passed to \code{\link[=audit_everything]{audit_everything()}} when \code{audit = TRUE}.}

\item{verbose}{Passed to \code{\link[=audit_everything]{audit_everything()}} when \code{audit = TRUE}.}
}
\value{
Invisibly, a list with \code{stub_path}, \code{index_updated}, \code{folder}, and
optional \code{audit}.
}
\description{
Reads the short registry yaml from the study repository (\verb{registry/} or
\verb{inst/registry/}), copies it to \verb{studies/<folder>.yml} in a registry
checkout, and rebuilds \code{index.csv} via \code{\link[=build_registry_index]{build_registry_index()}}.
}
\details{
This is a \strong{maintainer} function. Contributors should run
\code{\link[=prepare_study_for_registry]{prepare_study_for_registry()}} and open a pull request; maintainers run this
(or \code{\link[=refresh_registry]{refresh_registry()}}) from a local registry checkout.
}
\examples{
\dontrun{
options(replicateEverything.registry_root = "../registry")
sync_study_to_registry(".")
}

}
