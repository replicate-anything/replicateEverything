% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_actions.R
\name{shiny_run_action}
\alias{shiny_run_action}
\title{Shared Shiny Live Run button action}
\usage{
shiny_run_action(
  doi,
  what,
  language = NULL,
  repo = NULL,
  folder = NULL,
  install_deps = TRUE,
  study_root = NULL,
  on_wzb = NULL,
  estimate_limit_seconds = NULL,
  check_compatibility = TRUE,
  execute = TRUE
)
}
\arguments{
\item{doi}{Character. DOI of the paper.}

\item{what}{Replication identifier.}

\item{repo}{Optional repository slug.}

\item{folder}{Optional registry folder name.}

\item{install_deps}{Logical. Passed to live rendering.}

\item{study_root}{Optional local study root for audit runtime lookup.}

\item{on_wzb}{Whether this is a WZB Shiny host (hard estimate block).
Default: \code{\link[=shiny_running_on_wzb]{shiny_running_on_wzb()}}.}

\item{estimate_limit_seconds}{Interactive estimate limit; default from
\code{\link[=shiny_live_run_estimate_limit_seconds]{shiny_live_run_estimate_limit_seconds()}}.}

\item{check_compatibility}{Run \code{\link[=check_study_compatibility]{check_study_compatibility()}} before live.}

\item{execute}{When \code{FALSE}, only evaluate gates / estimate (for Shiny
modals + progress). When \code{TRUE}, also run the live replication.}
}
\value{
List with \code{ok}, \code{ok_to_execute}, \code{gate},
\code{message}, optional live result fields, and estimate metadata.
}
\description{
Gates (in order): \code{shiny_run: false}, padlock/hammer gap, study
compatibility, then long-run estimate. When gates pass and
\code{execute = TRUE}, runs leaf-only live display
(\code{prefer = "live"}) and resolves via \code{\link[=resolve_replication_display]{resolve_replication_display()}}.
}
\details{
\strong{Estimate gate:} when estimated runtime exceeds
\code{\link[=shiny_live_run_estimate_limit_seconds]{shiny_live_run_estimate_limit_seconds()}}, WZB sessions
(\code{on_wzb = TRUE}) hard-block; local/non-WZB sessions get a
\code{long_run_warning} but may still run (soft gate).
}
\seealso{
\code{\link[=shiny_display_action]{shiny_display_action()}}, \code{\link[=load_replication_for_display]{load_replication_for_display()}}
}
\keyword{internal}
