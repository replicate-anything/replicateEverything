% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stata_replication.R
\name{stata_executable_label}
\alias{stata_executable_label}
\title{Human-readable label for a resolved Stata executable path}
\usage{
stata_executable_label(path)
}
\arguments{
\item{path}{Absolute path to a Stata executable, or \code{NULL}.}
}
\value{
Character scalar, e.g. \code{"Stata 17 MP (C:/.../StataMP-64.exe)"}
or \code{"(Stata not found)"}.
}
\description{
Derives a short "Stata <version> <edition>" label from the
install path (e.g. \code{.../Stata17/StataMP-64.exe} -> \code{"Stata 17
MP"}), so check/install messages make it obvious at a glance whether they
resolved the same Stata. The raw path (always reported alongside) is the
ground truth for programmatic comparison.
}
\keyword{internal}
