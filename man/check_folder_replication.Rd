% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_folder_replication.R
\name{check_folder_replication}
\alias{check_folder_replication}
\title{Validate a folder-backed replication study}
\usage{
check_folder_replication(
  location = ".",
  full_replication = FALSE,
  registry_root = NULL
)
}
\arguments{
\item{location}{Local study path or GitHub address (\code{org/repo} or URL).
Defaults to the current working directory when it contains
\code{replication.yml}.}

\item{full_replication}{If \code{TRUE}, also run every table and figure via
\code{\link[=run_replication]{run_replication()}} and require success.}

\item{registry_root}{Optional path to the registry checkout (sibling of the
study repo in a monorepo). Defaults to
\code{getOption("replicateEverything.registry_root")}.}
}
\value{
A list with \code{ok} (logical), \code{checks} (data frame), and \code{study_path}.
}
\description{
Runs a transparent checklist: study layout, \code{replication.yml}, code and data
paths, baked display outputs under \verb{outputs/}, optional \verb{tests/testthat/},
substantive (published-value) checks under \verb{tests/substantive/}, and
(optionally) live execution of every table and figure.
}
\examples{
\dontrun{
check_folder_replication(".")
check_folder_replication(".", full_replication = TRUE)
}

}
