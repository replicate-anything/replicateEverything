% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stata_replication.R
\name{stata_probe_failure_code}
\alias{stata_probe_failure_code}
\title{Exit code that made a generated Stata probe fail, if attributable}
\usage{
stata_probe_failure_code(text)
}
\arguments{
\item{text}{Character scalar (e.g. \code{conditionMessage()} of the error
thrown by \code{run_stata_do()}, which includes the Stata error/log
tail).}
}
\value{
Character scalar exit code, or \code{NA_character_} if none found.
}
\description{
\code{stata_runner_lines()} always prints
\code{"...ended with error r(<code>);..."} when the wrapped do-file
aborts, and Stata itself echoes a bare \code{r(<code>);} line for the
underlying \code{exit <code>}. Either is enough to recover the first
failing exit code from the run's error text.
}
\keyword{internal}
