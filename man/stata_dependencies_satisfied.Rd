% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stata_replication.R
\name{stata_dependencies_satisfied}
\alias{stata_dependencies_satisfied}
\title{Whether required Stata SSC packages load without running install scripts}
\usage{
stata_dependencies_satisfied(
  study_root,
  staging_dir = NULL,
  timeout = 120L,
  meta = NULL
)
}
\arguments{
\item{staging_dir}{Optional writable directory for \code{$result} output.}

\item{timeout}{Seconds before aborting (best effort on Windows).}

\item{meta}{Parsed replication metadata.}
}
\value{
\code{TRUE}, \code{FALSE}, or \code{NA} (no probe configured).
}
\description{
Uses the study's \code{stata_deps_probe} script when declared; otherwise a
generic \code{which}-only probe from \code{stata_packages}. Returns
\code{NA} when neither is configured (caller should run install scripts or
skip per policy).
}
\keyword{internal}
