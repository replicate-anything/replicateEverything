% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_replication.R
\name{first_available_replication_row}
\alias{first_available_replication_row}
\title{First replication row suitable for Shiny default selection}
\usage{
first_available_replication_row(
  replications_df,
  doi,
  folder = NULL,
  repo = NULL,
  language_for_row = NULL,
  resolve_id = NULL
)
}
\arguments{
\item{replications_df}{Data frame from Shiny \code{replications_to_df()}.}

\item{doi}{Study DOI / key (for artifact lookup).}

\item{folder}{Optional local registry / study folder.}

\item{repo}{Optional study repo slug.}

\item{language_for_row}{Optional function \code{function(row) -> engine}.}

\item{resolve_id}{Optional function \code{function(row, engine) -> step id}.}
}
\value{
One-row data frame, or \code{NULL}.
}
\description{
Walks \code{replications_df} in order and returns the first row where
\code{shiny_step_default_available()} is true. Falls back to row 1 when none
qualify (or the frame is empty / NULL).
}
\keyword{internal}
