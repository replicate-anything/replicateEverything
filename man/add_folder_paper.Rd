% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_folder_paper.R
\name{add_folder_paper}
\alias{add_folder_paper}
\title{Add a folder-backed study to the replication registry (maintainer)}
\usage{
add_folder_paper(
  location = ".",
  full_replication = FALSE,
  registry_root = NULL,
  dry_run = FALSE,
  build_artifacts = FALSE,
  install_deps = TRUE,
  audit = FALSE
)
}
\arguments{
\item{location}{Local study path or GitHub address. Defaults to \code{"."}.}

\item{full_replication}{If \code{TRUE}, also run every table and figure live.}

\item{registry_root}{Path to the registry repository root. Defaults to
\code{getOption("replicateEverything.registry_root")}.}

\item{dry_run}{If \code{TRUE}, run checks only; do not write registry files.}

\item{build_artifacts}{If \code{TRUE}, run \code{\link[=build_study_outputs]{build_study_outputs()}} before checks.}

\item{install_deps}{Passed to \code{\link[=build_study_outputs]{build_study_outputs()}}.}

\item{audit}{If \code{TRUE}, run \code{\link[=audit_everything]{audit_everything()}} for this study after sync.}
}
\value{
Invisibly, the result of \code{\link[=check_replication]{check_replication()}}, with
\code{stub_path} and \code{index_updated} when registration succeeds.
}
\description{
Validates a folder-backed study with \code{\link[=check_replication]{check_replication()}}, then installs a
stub built from the study \code{replication.yml} into a registry checkout via
\code{\link[=sync_study_to_registry]{sync_study_to_registry()}}. Stub files are written only under the registry
repository — not into the study repo.
}
\details{
Contributors should run \code{\link[=prepare_study_for_registry]{prepare_study_for_registry()}} (validate / build).
Maintainers use this function (or \code{sync_study_to_registry()} directly) from a
local registry checkout.
}
\keyword{internal}
