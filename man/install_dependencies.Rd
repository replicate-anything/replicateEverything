% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintainer_dependencies.R
\name{install_dependencies}
\alias{install_dependencies}
\title{Install declared dependencies for a study, or for the whole registry}
\usage{
install_dependencies(
  what = ".",
  registry_root = NULL,
  repo = NULL,
  folder = NULL,
  verbose = TRUE
)
}
\arguments{
\item{what}{Study DOI, registry handle, local path, or GitHub slug
(default \code{"."}, the current working directory); or
\code{"everywhere"} to install dependencies for every study in the
registry index.}

\item{registry_root}{Optional registry checkout for monorepo dev.}

\item{repo, folder}{Optional registry row hints. Ignored when
\code{what = "everywhere"}.}

\item{verbose}{Logical. Print progress lines. Only used when
\code{what = "everywhere"}.}
}
\value{
Invisibly \code{TRUE} for a single study, or (when
\code{what = "everywhere"}) a named list of per-DOI results
(\code{ok} / \code{error}).
}
\description{
Single maintainer entry point for dependency setup. Mirrors the
\code{\link[=build_outputs]{build_outputs()}} / \code{\link[=validate_outputs]{validate_outputs()}} scope pattern: pass a study
location to install for one study, or \code{what = "everywhere"} to install
for every study in the registry index (see \code{\link[=load_index]{load_index()}}).
}
\examples{
\dontrun{
install_dependencies("10.1017/S0003055426101749")
install_dependencies("path/to/study-repo")
options(replicateEverything.registry_root = "../registry")
install_dependencies("everywhere")
}

}
\seealso{
\code{\link[=check_study_compatibility]{check_study_compatibility()}}, \code{\link[=build_study_outputs]{build_study_outputs()}},
\code{\link[=build_outputs]{build_outputs()}}, \code{\link[=validate_outputs]{validate_outputs()}}
}
