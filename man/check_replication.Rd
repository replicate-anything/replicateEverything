% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_folder_replication.R,
%   R/check_package_replication.R, R/check_replication.R
\name{check_folder_replication}
\alias{check_folder_replication}
\alias{check_package_replication}
\alias{check_replication}
\title{Validate a folder-backed replication study}
\usage{
check_folder_replication(
  location = ".",
  full_replication = FALSE,
  registry_root = NULL
)

check_package_replication(location, full_replication = FALSE)

check_replication(
  location = ".",
  full_replication = FALSE,
  registry_root = NULL
)
}
\arguments{
\item{location}{Local study path, GitHub address, or installed package path.
Defaults to the current working directory when it contains
\code{replication.yml} or \code{DESCRIPTION}. This is the \code{location} analog of
\code{doi = "local"} used by \code{\link[=list_replications]{list_replications()}}, \code{\link[=run_replication]{run_replication()}}, and
\code{\link[=get_code]{get_code()}} — both mean "the study checked out in the current working
directory"; no registry lookup is required.}

\item{full_replication}{If \code{TRUE}, also run every table and figure via
\code{\link[=run_replication]{run_replication()}} and require success.}

\item{registry_root}{Optional path to the registry checkout (folder studies
in a monorepo). Defaults to
\code{getOption("replicateEverything.registry_root")}.}
}
\value{
A list with \code{ok} (logical), \code{checks} (data frame), and \code{study_path}.

A list with \code{ok} (logical), \code{checks} (data frame), and \code{package_path}.

A list with \code{ok} (logical), \code{checks} (data frame), and \code{study_path}
or \code{package_path}.
}
\description{
Runs a transparent checklist: study layout, \code{replication.yml}, code and data
paths, resolvable code file links (\code{source()} / Stata \code{do}), baked display
outputs, optional \verb{tests/testthat/}, substantive
(published-value) checks under \verb{tests/substantive/}, and (optionally) live
execution of every table and figure.
}
\section{Functions}{
\itemize{
\item \code{check_folder_replication()}: Folder-backed implementation.

Runs a transparent checklist: study layout, \code{replication.yml}, code and data
paths, resolvable code file links (\code{source()} / Stata \code{do}), baked display
outputs under \verb{outputs/}, optional \verb{tests/testthat/}, substantive
substantive (published-value) checks under \verb{tests/substantive/}, and
(optionally) live execution of every table and figure.

\item \code{check_package_replication()}: Package-backed implementation.

Runs a transparent checklist: package layout, \code{replication.yml},
study \verb{make_*} / \verb{format_*} helpers, baked artifacts, substantive
(published-value) checks under \verb{tests/substantive/}, and (optionally)
live execution of every table and figure via replicateEverything.

}}
\examples{
\dontrun{
# setwd() to the study repo (or open its RStudio project) first, then:
setwd("path/to/rep-my-study")

# Quick manual smoke check before the full checklist:
list_replications("local")
describe_study_dag("local")
run_replication("local", "tab_1")  # one light step

check_replication(".")
check_replication(".", full_replication = TRUE)
check_replication("../rep-10.1371-journal.pone.0278337")
}

}
\keyword{internal}
