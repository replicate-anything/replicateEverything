# Whether a yaml entry is a pipeline / prep step

Whether a yaml entry is a pipeline / prep step

## Usage

``` r
is_prep_entry(rep)
```

## Arguments

- rep:

  Replication or prep entry from `replication.yml`.

## Value

Logical scalar.
