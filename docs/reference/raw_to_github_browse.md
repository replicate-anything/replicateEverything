# Convert a raw GitHub URL to a browseable `/blob/` link

Convert a raw GitHub URL to a browseable `/blob/` link

## Usage

``` r
raw_to_github_browse(url)
```

## Arguments

- url:

  Raw `raw.githubusercontent.com` URL.

## Value

Browse URL or original `url`.
