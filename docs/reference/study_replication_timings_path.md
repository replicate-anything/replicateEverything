# Path to study bake-timings JSON

Path to study bake-timings JSON

## Usage

``` r
study_replication_timings_path(study_root)
```

## Arguments

- study_root:

  Study repository root.

## Value

Character path (`outputs/replication_timings.json`).
