# Ungate interactive footer so eval(parse()) runs the call body (legacy)

Prefer
[`assemble_get_code_run_from_yaml()`](https://replicate-anything.github.io/replicateEverything/reference/assemble_get_code_run_from_yaml.md)
/
[`prepare_get_code_for_run()`](https://replicate-anything.github.io/replicateEverything/reference/prepare_get_code_for_run.md);
footers are optional and not required for Live Run.

## Usage

``` r
ungate_nframe_footer(lines)
```
