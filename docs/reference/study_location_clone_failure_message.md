# Message when GitHub clone fails but a local sibling may exist

Message when GitHub clone fails but a local sibling may exist

## Usage

``` r
study_location_clone_failure_message(slug, loc = slug)
```

## Arguments

- slug:

  GitHub `org/repo` slug that was cloned.

- loc:

  Original location string from the user.

## Value

Character scalar error message.
