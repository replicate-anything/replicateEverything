# Whether a proprietary / system engine is available on PATH

Light probe only (no installs). Ordinary engines (R / Stata / Python)
are treated as available here; their absence is handled by the usual run
path.

## Usage

``` r
system_engine_available(display_name)
```
