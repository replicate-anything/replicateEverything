# Path to a registry study stub yaml file

Path to a registry study stub yaml file

## Usage

``` r
registry_study_yaml_path(registry_root, folder)
```

## Arguments

- registry_root:

  Registry checkout root.

- folder:

  Registry folder name.

## Value

Character path under `studies/<folder>.yml`.
