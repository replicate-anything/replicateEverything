# Whether ctx redirects materials to a different repo than meta\$repo

Used for inherited steps on extension studies: `materials_repo` points
at the parent while `meta$repo` remains the extension slug.

## Usage

``` r
materials_repo_override(meta, ctx)
```
