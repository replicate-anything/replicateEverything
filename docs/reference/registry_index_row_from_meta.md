# Build a registry index row from study or registry stub metadata

Build a registry index row from study or registry stub metadata

## Usage

``` r
registry_index_row_from_meta(meta, study_root = NULL, folder = NULL)
```
