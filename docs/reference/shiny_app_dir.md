# Path to the bundled Shiny demo app

Path to the bundled Shiny demo app

## Usage

``` r
shiny_app_dir(package = "replicateEverything")
```

## Arguments

- package:

  Package name; default `"replicateEverything"`.

## Value

Normalized path to `inst/shiny`, or `""` if missing.
