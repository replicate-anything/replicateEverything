# Append a yaml-implied run expression (replication.yml is the execute recipe)

Append a yaml-implied run expression (replication.yml is the execute
recipe)

## Usage

``` r
assemble_get_code_run_from_yaml(rep, lines)
```
