# Look up last known bake seconds for a step id

Look up last known bake seconds for a step id

## Usage

``` r
lookup_study_replication_timing(study_root, step_id)
```

## Arguments

- study_root:

  Study root.

- step_id:

  Step / replication id.

## Value

Numeric seconds, or `NA_real_` when unknown.
