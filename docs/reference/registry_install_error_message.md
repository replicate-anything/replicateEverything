# Format a registry bulk-install failure with row context

Format a registry bulk-install failure with row context

## Usage

``` r
registry_install_error_message(
  error,
  row,
  i,
  n,
  location,
  repo = NULL,
  folder = NULL
)
```
