# Raw GitHub URLs for a study package `replication.yml`

Raw GitHub URLs for a study package `replication.yml`

## Usage

``` r
package_replication_yaml_urls(repo, ref = "main")
```

## Arguments

- repo:

  GitHub slug `org/repo`.

- ref:

  Branch or tag.

## Value

Character vector of raw URLs (tried in order).
