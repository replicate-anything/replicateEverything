# Read text lines from a local path or HTTP(S) URL

Read text lines from a local path or HTTP(S) URL

## Usage

``` r
read_lines_url(url)
```

## Arguments

- url:

  Character URL or file path.

## Value

Character vector of lines, or empty if not found.
