# Whether an error is the generic empty-`steps:` normalize failure

Whether an error is the generic empty-`steps:` normalize failure

## Usage

``` r
is_missing_steps_normalize_error(e)
```

## Arguments

- e:

  A condition.
