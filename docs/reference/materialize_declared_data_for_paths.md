# Materialize declared remote sources for missing study data paths

Materialize declared remote sources for missing study data paths

## Usage

``` r
materialize_declared_data_for_paths(
  rel_paths,
  study_root,
  meta,
  ctx = NULL,
  force = FALSE
)
```
