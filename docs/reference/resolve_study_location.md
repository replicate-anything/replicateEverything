# Resolve a folder-backed study repository root

Resolve a folder-backed study repository root

## Usage

``` r
resolve_study_location(location)
```

## Arguments

- location:

  Local study path or GitHub address (`org/repo` or URL).

## Value

Normalized path to study repo root (contains `replication.yml`).
