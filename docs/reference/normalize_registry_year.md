# Normalize paper year for registry index (keeps bibliography suffixes like 2026a)

Normalize paper year for registry index (keeps bibliography suffixes
like 2026a)

## Usage

``` r
normalize_registry_year(year)
```
