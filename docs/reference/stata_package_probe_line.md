# Stata do-file line(s) that test presence of a declared package

Stata do-file line(s) that test presence of a declared package

## Usage

``` r
stata_package_probe_line(pkg)
```

## Arguments

- pkg:

  Declared package name from `stata_packages:`.

## Value

Character scalar: a single `cap which ...` or `cap findfile ...` line.
