# Whether a path is raw study data (under `data/`, not a step output)

Whether a path is raw study data (under `data/`, not a step output)

## Usage

``` r
is_root_data_path(path, produced_outputs = character(0))
```
