# Prefer a runnable path, then one whose languages include R

Prefer a runnable path, then one whose languages include R

## Usage

``` r
default_path_selector_language(entries, paper_meta = NULL)
```
