# Noun for get_code / Code-tab run tips (table / figure / step / result)

Noun for get_code / Code-tab run tips (table / figure / step / result)

## Usage

``` r
get_code_result_kind(type = NULL)
```
