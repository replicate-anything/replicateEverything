# Read `audit_jobs.csv` from the registry

Read `audit_jobs.csv` from the registry

## Usage

``` r
read_registry_audit_jobs(registry_root = NULL)
```

## Arguments

- registry_root:

  Optional registry root.

## Value

Data frame (possibly empty) with canonical columns.
