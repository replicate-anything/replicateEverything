# Label and title prefix for a feedback category

Label and title prefix for a feedback category

## Usage

``` r
shiny_feedback_category_meta(category)
```

## Arguments

- category:

  Allowlisted category.

## Value

Named list with `label` and `title_prefix`.
