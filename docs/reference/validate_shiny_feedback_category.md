# Validate a Shiny feedback category against the allowlist

Validate a Shiny feedback category against the allowlist

## Usage

``` r
validate_shiny_feedback_category(category)
```

## Arguments

- category:

  Character scalar.

## Value

Allowlisted category, or `NA_character_` when invalid.
