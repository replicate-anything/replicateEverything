# Default display formatting when no `format_*` function is defined

Default display formatting when no `format_*` function is defined

## Usage

``` r
default_format_object(object, rep, paper_meta = NULL)
```

## Arguments

- object:

  Analysis or Stata output object.

- rep:

  Replication entry.

- paper_meta:

  Optional paper-level metadata.
