# Installed package library path

Installed package library path

## Usage

``` r
package_library_path(package = "replicateEverything")
```

## Arguments

- package:

  Package name.

## Value

Normalized path to the installed package directory, or `""`.
