# Whether a step engine satisfies a language selector

Surgical Dataverse access steps (`engine: dataverse`) are hosted in R
helpers, so they match both `"dataverse"` and `"r"`.

## Usage

``` r
replication_engine_matches_language(engine, language)
```
