# Normalize a source_repository yaml value to a character vector

Accepts a scalar string, a yaml sequence (list), or a character vector.
Empty / NA entries are dropped.

## Usage

``` r
normalize_source_repository_values(val)
```
