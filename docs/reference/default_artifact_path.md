# Resolve the expected artifact path for a replication entry

Resolve the expected artifact path for a replication entry

## Usage

``` r
default_artifact_path(rep, what)
```

## Arguments

- rep:

  A single replication entry from `replication.yml`.

- what:

  Replication identifier.
