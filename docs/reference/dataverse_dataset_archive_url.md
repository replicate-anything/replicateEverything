# URL for downloading a full dataset archive from Harvard Dataverse

URL for downloading a full dataset archive from Harvard Dataverse

## Usage

``` r
dataverse_dataset_archive_url(
  dataset,
  server = "dataverse.harvard.edu",
  original = TRUE
)
```
