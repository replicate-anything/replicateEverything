# Whether a study's Stata dependency scripts already ran this session

Whether a study's Stata dependency scripts already ran this session

## Usage

``` r
stata_deps_installed_this_session(deps_key)
```

## Arguments

- deps_key:

  Character key identifying the study + its install scripts.

## Value

Logical scalar.
