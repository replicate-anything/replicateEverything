# Study-level padlock / hammer flags from step entries

Study-level padlock / hammer flags from step entries

## Usage

``` r
study_gap_flags_from_entries(entries)
```

## Arguments

- entries:

  List of step entries (or parsed meta with `steps`).

## Value

List with logical `data_unavailable` and `missing_engine`.
