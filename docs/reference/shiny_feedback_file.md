# Resolve configured Shiny feedback CSV path (relative or absolute)

Reads `REPLICATE_SHINY_FEEDBACK_FILE` or
`options(replicate_shiny.feedback_file)`; default `data/feedback.csv`.

## Usage

``` r
shiny_feedback_file()
```

## Value

Character scalar file path.
