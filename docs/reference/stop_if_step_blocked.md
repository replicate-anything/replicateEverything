# Stop with a clear, informative message when a step cannot be created

Stop with a clear, informative message when a step cannot be created

## Usage

``` r
stop_if_step_blocked(meta, what, output_exists = NULL)
```
