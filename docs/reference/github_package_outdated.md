# Whether an installed package lags the GitHub ref

Whether an installed package lags the GitHub ref

## Usage

``` r
github_package_outdated(package, repo, ref = "main")
```

## Arguments

- package:

  Installed package name.

- repo:

  GitHub slug `org/repo`.

- ref:

  Branch, tag, or commit.
