# Resolve a study root from common alias strings (DOI, registry folder, rep- slug)

Resolve a study root from common alias strings (DOI, registry folder,
rep- slug)

## Usage

``` r
try_resolve_study_by_common_alias(loc)
```

## Arguments

- loc:

  Trimmed location string.

## Value

Normalized study path or `NULL`.
