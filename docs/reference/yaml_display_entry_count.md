# Count display (table/figure) entries in parsed study yaml

Prefers unified `steps:`; falls back to legacy `replications:`.

## Usage

``` r
yaml_display_entry_count(parsed)
```

## Arguments

- parsed:

  Parsed yaml list.

## Value

Integer count.
