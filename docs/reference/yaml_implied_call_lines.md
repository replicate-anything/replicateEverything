# Compact yaml-implied call lines (no section banner) for tips / recipes

Shared by
[`emit_get_code_usage_message()`](https://replicate-anything.github.io/replicateEverything/reference/emit_get_code_usage_message.md)
and
[`assemble_get_code_run_from_yaml()`](https://replicate-anything.github.io/replicateEverything/reference/assemble_get_code_run_from_yaml.md).

## Usage

``` r
yaml_implied_call_lines(rep, lines = NULL)
```
