# Resolve a Stata path (alias for `resolve_code_path`)

Resolve a Stata path (alias for `resolve_code_path`)

## Usage

``` r
resolve_stata_path(path, study_root, globals = character(), from_file = NULL)
```
