# Whether any declared display output for a step already exists

Whether any declared display output for a step already exists

## Usage

``` r
step_display_output_exists(
  doi,
  what,
  repo = NULL,
  folder = NULL,
  language = NULL
)
```
