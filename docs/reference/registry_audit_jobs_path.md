# Path to the registry audit jobs CSV

Path to the registry audit jobs CSV

## Usage

``` r
registry_audit_jobs_path(registry_root = NULL)
```

## Arguments

- registry_root:

  Optional registry repository root.

## Value

Character path, or `""` if the registry root is unknown.
