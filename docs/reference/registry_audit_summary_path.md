# Path to the registry audit summary JSON

Path to the registry audit summary JSON

## Usage

``` r
registry_audit_summary_path(registry_root = NULL)
```

## Arguments

- registry_root:

  Optional registry repository root.

## Value

Character path, or `""` if the registry root is unknown.
