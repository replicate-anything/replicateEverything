# Legacy GitHub raw URLs for registry study stubs

Legacy GitHub raw URLs for registry study stubs

## Usage

``` r
registry_study_yaml_url_legacy(
  folder,
  registry_repo = DEFAULT_REGISTRY_REPO,
  ref = "main"
)
```
