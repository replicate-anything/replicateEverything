# Whether a replication entry is a surgical Dataverse access step

Whether a replication entry is a surgical Dataverse access step

## Usage

``` r
is_dataverse_replication(rep, paper_meta = NULL)
```
