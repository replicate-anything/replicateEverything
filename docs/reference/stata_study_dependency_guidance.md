# Study-specific Stata dependency guidance from replication.yml

Study-specific Stata dependency guidance from replication.yml

## Usage

``` r
stata_study_dependency_guidance(study_root, meta = NULL)
```
