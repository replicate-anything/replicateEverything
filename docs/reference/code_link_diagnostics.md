# Diagnostic context for a code link resolution attempt

Diagnostic context for a code link resolution attempt

## Usage

``` r
code_link_diagnostics(
  resolved,
  study_root,
  globals = character(),
  allowed_root = study_root
)
```
