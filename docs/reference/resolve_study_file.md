# Resolve a repo-relative path, including base-study outputs for extensions

Resolve a repo-relative path, including base-study outputs for
extensions

## Usage

``` r
resolve_study_file(path, ctx, meta = NULL, local_only = FALSE, step = NULL)
```
