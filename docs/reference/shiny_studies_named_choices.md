# Named character vector for `selectInput(choices=)` from baked UI pairs

Named character vector for `selectInput(choices=)` from baked UI pairs

## Usage

``` r
shiny_studies_named_choices(pairs)
```

## Arguments

- pairs:

  List of lists with `value` and `label`.

## Value

Named character vector (`names` = labels, values = values).
