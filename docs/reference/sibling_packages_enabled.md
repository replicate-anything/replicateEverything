# Whether to resolve study packages from sibling monorepo folders

Off by default on servers; enable via `local.R` or
`replicateEverything.use_sibling_packages`.

## Usage

``` r
sibling_packages_enabled()
```
