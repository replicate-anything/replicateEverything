# Whether replication metadata refers to an installed R package

Whether replication metadata refers to an installed R package

## Usage

``` r
is_package_replication(meta)
```

## Arguments

- meta:

  Parsed replication.yml contents.

## Value

Logical.
