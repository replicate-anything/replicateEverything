# Whether a location string looks like a study alias, not an R package name

Whether a location string looks like a study alias, not an R package
name

## Usage

``` r
looks_like_study_alias(x)
```

## Arguments

- x:

  Character scalar.

## Value

Logical scalar.
