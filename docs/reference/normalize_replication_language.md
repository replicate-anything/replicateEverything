# Normalize a replication language argument

Normalize a replication language argument

## Usage

``` r
normalize_replication_language(language)
```

## Arguments

- language:

  `"R"`, `"r"`, `"Stata"`, or `"stata"`.

## Value

`"r"`, `"stata"`, or `NULL`.
