# Run a Pattern B Dataverse access step (surgical file pulls → outputs/)

Run a Pattern B Dataverse access step (surgical file pulls → outputs/)

## Usage

``` r
run_dataverse_access_step(rep, study_root, meta = NULL, force = TRUE)
```
