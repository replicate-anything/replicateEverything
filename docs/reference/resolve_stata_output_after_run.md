# Resolve Stata output path after a run

Resolve Stata output path after a run

## Usage

``` r
resolve_stata_output_after_run(rep, study_root, staging_dir = NULL)
```

## Arguments

- rep:

  Replication entry.

- study_root:

  Study repository root.

- staging_dir:

  Optional writable staging directory.
