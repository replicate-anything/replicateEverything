# Locate monorepo root from a starting directory

Locate monorepo root from a starting directory

## Usage

``` r
monorepo_root_from_path(path)
```

## Arguments

- path:

  Directory path (e.g. session launch directory).

## Value

Normalized monorepo path or `NULL`.
