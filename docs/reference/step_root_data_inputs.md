# Raw `data/` inputs for a step (excludes outputs from upstream steps)

Raw `data/` inputs for a step (excludes outputs from upstream steps)

## Usage

``` r
step_root_data_inputs(step, produced_outputs = character(0))
```
