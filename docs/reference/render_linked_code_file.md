# Render a linked code file for the Shiny code viewer

Render a linked code file for the Shiny code viewer

## Usage

``` r
render_linked_code_file(
  rel_path,
  study_root,
  language = c("stata", "r"),
  read_fn = NULL,
  globals = NULL
)
```
