# Human path label from language tokens (e.g. `"Stata / R"`)

Human path label from language tokens (e.g. `"Stata / R"`)

## Usage

``` r
format_path_languages_label(languages)
```

## Arguments

- languages:

  Character vector of language tokens.

## Value

Character scalar suitable for a Shiny path box.
