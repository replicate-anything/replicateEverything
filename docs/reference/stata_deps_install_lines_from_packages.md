# Build a Stata install script from `stata_packages:`

Build a Stata install script from `stata_packages:`

## Usage

``` r
stata_deps_install_lines_from_packages(packages)
```

## Arguments

- packages:

  Character vector of SSC ado names.

## Value

Character vector of Stata commands.
