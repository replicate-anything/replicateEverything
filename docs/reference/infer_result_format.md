# Infer the format of a replication result object

Infer the format of a replication result object

## Usage

``` r
infer_result_format(object, type)
```

## Arguments

- object:

  Result returned by a replication script.

- type:

  Replication type (`figure` or `table`).

## Value

Character string describing the result format.
