# Code language for display (Shiny / get_code)

Code language for display (Shiny / get_code)

## Usage

``` r
replication_code_language(rep, paper_meta = NULL)
```

## Arguments

- rep:

  Replication entry.

- paper_meta:

  Optional paper metadata.

## Value

`"stata"` or `"r"`.
