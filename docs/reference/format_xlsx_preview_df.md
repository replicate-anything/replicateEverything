# Trim blank rows/cols and round numeric cells for Excel Display preview

Trim blank rows/cols and round numeric cells for Excel Display preview

## Usage

``` r
format_xlsx_preview_df(df)
```

## Arguments

- df:

  A data frame from `readxl::read_excel(..., col_names = FALSE)`.

## Value

A character data frame (possibly empty).
