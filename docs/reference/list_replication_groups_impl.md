# One replication entry per logical group (internal)

One replication entry per logical group (internal)

## Usage

``` r
list_replication_groups_impl(
  meta,
  entries = NULL,
  language = NULL,
  display_only = TRUE
)
```
