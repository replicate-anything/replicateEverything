# Build a display object for a prep step when no HTML artifact exists

Build a display object for a prep step when no HTML artifact exists

## Usage

``` r
load_prep_step_display(meta, ctx, prep)
```
