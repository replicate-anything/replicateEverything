# Whether an audit skip / error snippet is a missing-engine reason

Whether an audit skip / error snippet is a missing-engine reason

## Usage

``` r
audit_reason_is_missing_engine(reason)
```

## Arguments

- reason:

  Character snippet from audit `error_snippet` or skip reason.

## Value

`TRUE` when the text matches missing-engine phrasing.
