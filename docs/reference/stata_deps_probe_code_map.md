# Exit-code -\> package name map for [`stata_deps_probe_lines_from_packages()`](https://replicate-anything.github.io/replicateEverything/reference/stata_deps_probe_lines_from_packages.md)

Exit-code -\> package name map for
[`stata_deps_probe_lines_from_packages()`](https://replicate-anything.github.io/replicateEverything/reference/stata_deps_probe_lines_from_packages.md)

## Usage

``` r
stata_deps_probe_code_map(packages)
```

## Arguments

- packages:

  Character vector of ado command names.

## Value

Named character vector (exit code as name, package as value).
