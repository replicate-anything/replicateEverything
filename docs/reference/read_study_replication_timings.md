# Read study bake timings

Read study bake timings

## Usage

``` r
read_study_replication_timings(study_root)
```

## Arguments

- study_root:

  Study repository root (or NULL to return empty).

## Value

List with `generated_at` and `steps` (named list of records with at
least `seconds`).
