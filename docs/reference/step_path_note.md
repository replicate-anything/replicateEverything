# Optional short note for a multi-language path box (`path_note:`)

Surfaced in Shiny path-box tooltips and a visible caption under the
icons (e.g. that R is a translation of an original Mathematica kernel).

## Usage

``` r
step_path_note(step)
```

## Arguments

- step:

  Step entry.

## Value

Character scalar (possibly empty).
