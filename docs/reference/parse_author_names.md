# Split a comma-separated author list without breaking "Last, First" pairs

Split a comma-separated author list without breaking "Last, First" pairs

## Usage

``` r
parse_author_names(authors_str)
```
