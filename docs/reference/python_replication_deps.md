# Declared pip packages for a Python replication run

Merges entry-level `dependencies` with study-wide `python_dependencies:`
from `replication.yml`.

## Usage

``` r
python_replication_deps(rep, meta = NULL)
```
