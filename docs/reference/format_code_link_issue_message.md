# Format a human-readable code-link validation error

Format a human-readable code-link validation error

## Usage

``` r
format_code_link_issue_message(
  caller_rel,
  line,
  command,
  raw_path,
  resolved,
  study_root
)
```
