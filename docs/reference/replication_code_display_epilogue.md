# Epilogue notes when a script defines make\_\* but never calls it

Epilogue notes when a script defines make\_\* but never calls it

## Usage

``` r
replication_code_display_epilogue(rep, lines)
```
