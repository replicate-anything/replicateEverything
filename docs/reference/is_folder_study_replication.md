# Whether replication metadata refers to a folder-backed external study repo

Whether replication metadata refers to a folder-backed external study
repo

## Usage

``` r
is_folder_study_replication(meta, ctx = NULL)
```

## Arguments

- meta:

  Parsed replication.yml contents.

- ctx:

  Optional paper context list with `repo` and `folder`.

## Value

Logical.
