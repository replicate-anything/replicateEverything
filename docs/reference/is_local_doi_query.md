# Detect whether a DOI argument requests the local working-directory study

Detect whether a DOI argument requests the local working-directory study

## Usage

``` r
is_local_doi_query(doi)
```

## Arguments

- doi:

  Character. Use `""`, `"local"`, or `"."`.

## Value

Logical scalar.
