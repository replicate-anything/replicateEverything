# Logical replication id (group) for a yaml entry

Logical replication id (group) for a yaml entry

## Usage

``` r
replication_logical_id(rep)
```

## Arguments

- rep:

  Replication entry from `replication.yml`.

## Value

Character scalar.
